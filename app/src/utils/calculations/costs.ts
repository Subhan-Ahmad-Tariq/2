export const calculateWaterCost = (usage: number, rate: number) => {
    return usage * rate; // Total cost = usage * rate
  };
  
  export const calculatePowerCost = (usage: number, rate: number) => {
    return usage * rate; // Total cost = usage * rate
  };