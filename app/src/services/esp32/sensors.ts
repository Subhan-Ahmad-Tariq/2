export const readSensorData = async () => {
    // Implement the logic to read sensor data from the ESP32 device
    console.log('Reading sensor data from ESP32...');
    // Example: Use BLE or MQTT to fetch sensor data
  };