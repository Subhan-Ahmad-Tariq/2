export const sendCommandToDevice = async (command: string) => {
    // Implement the logic to send commands to the ESP32 device
    console.log(`Sending command: ${command}`);
    // Example: Use BLE or MQTT to send the command
  };